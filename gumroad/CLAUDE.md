Build and publish a custom landing page for my Gumroad profile (@jenueldev). Design a unique, on-brand page — fully responsive, with light and dark mode. Preview it with `gumroad pages preview`, then publish with `gumroad pages push profile`. The page replaces my entire profile, so link visitors to my product pages instead of adding checkout elements. If I want an email signup, add a `<form data-gumroad-follow>` with an email input and an element marked `data-gumroad-follow-message` for the confirmation text — Gumroad wires the form to my email audience automatically.

# Design
./DESIGN.md